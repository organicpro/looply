import { useState } from 'react';
import { Logo } from './Logo';
export function LoginView({onLogin}:{onLogin:(name:string,photo:string)=>void}){const [name,setName]=useState('');const [photo,setPhoto]=useState('');return <div className="min-h-screen flex items-center justify-center p-6 bg-slate-950"><form onSubmit={e=>{e.preventDefault();if(name.trim())onLogin(name.trim(),photo)}} className="w-full max-w-md glass-panel rounded-[2rem] p-8 space-y-6"><div className="flex justify-center"><Logo size={64}/></div><h1 className="text-2xl font-display font-black text-white text-center">Bem-vindo ao Looply</h1><p className="text-sm text-slate-400 text-center">Crie seu perfil para continuar.</p><label className="block text-xs font-bold text-slate-300">Seu nome<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Como devemos chamar você?" className="mt-2 w-full rounded-xl px-4 py-3 bg-white/5 border border-white/10 text-white"/></label><label className="block text-xs font-bold text-slate-300">Foto de perfil<span className="mt-2 flex items-center gap-3"><span className="w-14 h-14 rounded-full overflow-hidden bg-gradient-to-br from-pink-500 to-rose-700 flex items-center justify-center text-white font-black">{photo?<img src={photo} className="w-full h-full object-cover"/>:name.charAt(0).toUpperCase()}</span><input type="file" accept="image/*" onChange={e=>{const file=e.target.files?.[0];if(file){const r=new FileReader();r.onload=()=>setPhoto(String(r.result));r.readAsDataURL(file)}}} className="text-xs text-slate-400"/></span></label><button className="w-full py-3.5 rounded-xl bg-gradient-to-r from-pink-500 to-rose-700 text-white font-black">Entrar</button></form></div>}
